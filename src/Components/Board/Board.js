import React from 'react';

const Board = props => {
    console.log('board props', props)
    // const { title, author } = props;
    return (
        <button>
            {/* <h3>{title}</h3>
            <h4>{author}</h4> */}
        </button>
    );
};

export default Board;

